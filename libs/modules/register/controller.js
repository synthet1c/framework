var
	User       = require( './model' );
	// UserSchema = require( './schema' );

module.exports = {

	/** GET /register */
	index: function( req, res, Template ){
		return function(){

		}
	},

	/** POST /register */
	register: function( req, res, Template ){
		return function( userObj ){

		}
	}
};